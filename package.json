{
  "name": "web",
  "version": "1.0.0",
  "scripts": {
    "dev": "concurrently \"bun run --cwd backend dev\" \"bun run --cwd frontend dev\"",
    "build": "npm install --prefix backend && npm install --prefix frontend && node frontend/node_modules/vite/bin/vite.js build --config frontend/vite.config.js",
    "start": "bun run --cwd backend start",
    "preview": "bun run build && bun run --cwd backend start"
  },
  "devDependencies": {
    "concurrently": "^9.1.2"
  },
  "dependencies": {
    "backend": "^0.0.0"
  }
}